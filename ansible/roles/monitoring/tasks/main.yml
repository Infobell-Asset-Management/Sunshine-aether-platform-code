---
- name: Create monitoring namespace
  shell: kubectl create namespace monitoring --kubeconfig /etc/kubernetes/admin.conf || true

- name: Apply Prometheus and Alertmanager manifests
  shell: kubectl apply -f /opt/aether/manifests/monitoring/ --kubeconfig /etc/kubernetes/admin.conf
